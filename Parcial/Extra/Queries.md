
Acuerdos
- Definir aliases en SELECT, no es necesario poner `AS` en todos los casos.
- Poner `DISTINCT`
- Nunca usar `WHERE` como reemplazo de `JOIN` porque puede generar un `CROSS JOIN` accidental.

No entran
- Unión/ consultas concatenadas
- Funciones de agregación (`SUM()`, `AVG()`, `COUNT()`, `MIN()`, `MAX()`)

No aparecen pero existen
- CROSS JOIN: combina cada fila de una tabla con todas las filas de otra.
- Full JOIN
- Right JOIN


Definiciones
- INNER JOIN = JOIN
- Left JOIN
- BETWEEN
- LIKE 'A%'
- IS NULL
- IN
- CASE
- SELECT *
- ORDER BY  ASC o DESC
- LIMIT
- LIMIT + OFFSET




JOIN

Left JOIN

Subquery





